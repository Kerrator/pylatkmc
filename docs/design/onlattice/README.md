# On-lattice Phase C design corpus — versioned snapshot

**Snapshot date: 2026-07-22** (corpus state after the Phase C first validation leg; includes one
2026-07-22 artifact, `E_sym_theta_2026-07-22.csv`, from the in-progress implementation leg).

The **live working copy remains `~/pykmc/onlattice_design/`** (the meta-root is deliberately not a
git repo) until the Phase C first leg completes — a follow-up sync commit will reconcile this
snapshot with the final state of that directory. Do not treat this tree as the editing surface.

**`PHASEC_RATE_MODEL_DECISION.md` is the approved architecture decision** (APPROVED 2026-07-21;
see its **§8** for the binding sign-off notes N1–N8). It amends `FINAL_DESIGN.md` §6 and
`phaseA_contract.md` §2.5. Gate verdicts and conditions stamps are in
`PHASEC_VALIDATION_REPORT_2026-07-21.md`. Subdirectories `designs/`, `judging/`, and `redteam/`
preserve the design-competition, judging, and red-team records that led to the final design.
